[package]
name = "orbis-identity-personhood-runtime-api"
description = "Bounded native identity and personhood runtime API for Orbis"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
codec = { features = ["derive", "max-encoded-len"], workspace = true }
scale-decode = { version = "0.16", default-features = false, features = ["derive"] }
scale-info = { features = ["derive"], workspace = true }
sp-api = { workspace = true }

[features]
default = ["std"]
std = ["codec/std", "scale-info/std", "sp-api/std"]
