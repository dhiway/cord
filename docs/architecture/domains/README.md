# Domain architecture contracts

Each domain document has exactly 21 numbered rows matching the canonical plan. `Target contract`
and `Evidence plan` remain concrete. Implementation cells distinguish source/runtime/SDK presence
from the still-pending P6/P7 acceptance evidence; implementation presence alone is not a
production-readiness claim. `not-applicable` must include a reason. Unknown/TBD values fail
validation.
