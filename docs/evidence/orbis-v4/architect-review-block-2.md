# Architect second BLOCK — remediation record

The second review blocked v4 on protocol-migration conflation, synthetic command-output hashes,
weak assertion mapping, incomplete row enumeration, insufficient adversarial tests, and no-hash
environment contamination. This records the block only. Architect and Critic clearance are both
still pending; Gate 5 must not be represented as frozen until those reviews exist.
