# `@cord-network/origin-sdk-crypto`

Portable, dependency-free cryptographic primitives shared by Origin SDK packages. The package performs deterministic byte hashing only; wallet key custody and transaction approval remain host responsibilities.
