# @cord-network/origin-sdk-descriptors

Fail-closed Commons network bindings and the metadata-generated PAPI descriptor. Import the full
metadata surface from `@cord-network/origin-sdk-descriptors/commons`; application code should prefer
the admitted domain packages rather than raw pallet calls.
