# @cord-network/origin-sdk-signer

Host and injected signer providers with explicit approval per transaction. The package accepts injected host or transport implementations and never selects an endpoint.

`@cord-network/origin-sdk-signer/testing` exports a framework-neutral signer with configurable
accounts and signatures, rejection injection, an immutable request log, and reset support.
